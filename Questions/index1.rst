100 React Native Practice Questions
State Management & Hooks (25 questions)

Create a counter component with increment, decrement, and reset functionality
Build a toggle switch component that manages its own state
Create a form with multiple input fields that tracks all values in a single state object
Build a shopping cart that calculates total price using useState and useEffect
Create a theme switcher (light/dark mode) using useContext
Build a multi-step form wizard that tracks current step and form data
Create a todo list with add, delete, and mark complete functionality
Build a search filter component that filters a list in real-time
Create a component that fetches user data on mount using useEffect
Build a debounced search input using custom hooks
Create a component with undo/redo functionality using useReducer
Build a timer component with start, pause, and reset controls
Create a favorites list that persists data using AsyncStorage
Build a component that tracks window dimensions on resize
Create a form validation system using custom hooks
Build a pagination component that manages current page state
Create a modal manager using useContext and useReducer
Build a component that implements optimistic UI updates
Create a global notification system using Context API
Build a component with infinite scroll functionality
Create a authentication state manager using useContext
Build a component that syncs state with URL parameters
Create a drag-and-drop list with reorderable items
Build a component that implements local state caching
Create a settings panel that saves preferences using AsyncStorage

Component Creation (30 questions)

Create a custom button component with loading and disabled states
Build a reusable card component with header, body, and footer slots
Create an avatar component with image fallback to initials
Build a badge component with different variants (success, error, warning)
Create a chip component with dismiss functionality
Build a progress bar component with percentage display
Create a skeleton loader component for content loading states
Build a rating component with star selection
Create an accordion component with expand/collapse functionality
Build a tabs component with swipeable content
Create a bottom sheet component with drag gestures
Build a dropdown select component with search
Create a date picker component with calendar view
Build a time picker component with hour/minute selection
Create a color picker component
Build a slider component with min/max values
Create a stepper component for numeric input
Build a checkbox group component
Create a radio button group component
Build a file upload component with preview
Create a video player component with controls
Build an image carousel with swipe navigation
Create a pull-to-refresh component
Build a floating action button component
Create a breadcrumb navigation component
Build a tag input component for multiple entries
Create a mentions input component (@mentions)
Build a code snippet display component
Create a markdown renderer component
Build a QR code generator component

Third-Party Libraries Integration (25 questions)

Integrate React Navigation and create a stack navigator
Build a bottom tab navigator with custom icons
Create a drawer navigation with user profile
Integrate React Native Maps and display user location
Build a map component with custom markers
Integrate Axios and create a data fetching service
Build an API client using React Query
Integrate Firebase Authentication
Create a real-time chat using Firebase Firestore
Integrate React Native Camera and build a photo capture screen
Build a barcode/QR scanner using React Native Camera
Integrate AsyncStorage and create a persistent state hook
Build a secure storage wrapper using react-native-keychain
Integrate Formik for form handling
Build a complex form with Yup validation
Integrate React Native Gesture Handler for swipe actions
Build a swipeable card stack using gesture handler
Integrate React Native Reanimated for smooth animations
Create an animated onboarding screen
Integrate Lottie for animated illustrations
Build a splash screen with Lottie animation
Integrate React Native Vector Icons
Create an icon picker component
Integrate React Native Paper UI library
Build a screen using Material Design components

Advanced Components & Patterns (20 questions)

Create a virtualized list with FlatList optimization
Build a sectioned list with headers using SectionList
Create an infinite loading list with pagination
Build a search results list with highlighting
Create a pull-to-refresh list with loading states
Build a custom calendar component with date selection
Create a timeline component for events
Build a chart component using react-native-chart-kit
Create a data table with sorting and filtering
Build a Kanban board with draggable cards
Create a rich text editor component
Build a signature capture component
Create a drawing canvas component
Build a audio recorder component
Create a biometric authentication component
Build a push notification handler
Create a deep linking handler component
Build a network status indicator
Create a permission request component
Build a app update checker component


Practice Tips:

Start with simpler state management questions before moving to complex ones
Focus on reusability and prop handling when creating components
Read library documentation thoroughly before integration
Test components with different screen sizes and orientations
Consider accessibility in all component designs